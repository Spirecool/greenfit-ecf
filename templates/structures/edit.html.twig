{% extends 'base.html.twig' %}

{% block title %}Éditer la structure {% endblock %}

{% block body %}
    <h1>Éditer la structure : {{ structure.name }}</h1>

    {{ include('structures/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="pt-4">
        <i class="fa-solid fa-circle-arrow-left pl-3"></i>
        <a href="{{ path('app_structures_index') }}">Revenir à la liste</a>
    </div>

    {{ include('structures/_delete_form.html.twig') }}
{% endblock %}
