{% extends 'loginpage.html.twig' %}

{% block title %}Se connecter à Green FIT{% endblock %}


{% block body %}

<div class="homepage">
  <div class="login-container banner-heading">
        <div> <img src="/images/logo.png">
        </div>
        <form action="{{ path('login') }}" method="post">
            <div class="form-group">
                    <label for="username">Adresse email:</label>
                    <input type="email" id="username" class="form-control form-control-sm" name="_username" aria-describedby="emailHelp" value="{{ last_username }}" >
            </div>

            <div class="form-group">
                            <label for="password">Mot de passe:</label>
                            <input type="password" id="password" name="_password"  class="form-control form-control-sm"/>
                            <input type="hidden"  name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                            <input type="hidden" name="_target_path" value="{{ path('app_home') }}"/>
                            <button class="btn btn-outline-dark btn-lg px-5 mt-5 login-button" type="submit">Se connecter</button>
                            <input type="hidden" name="_target_path" value="/admin"/> 
            </div>
        </form>
        {% if error %}
        <div class="text-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
  </div>
</div>
{# 
 <div id="coco">

    <div class="global-container flex  ">
        <div class="card login-form">
            <div class="card-body">
                <div class="card-body p-5 text-center">
                        
                    <h2 class="fw-bold mb-2 fs-3 text-uppercase">Se connecter</h2>
                    <p class="mb-5">Veuillez renseigner votre login et mot de passe</p>
                    <form action="{{ path('login') }}" method="post">
                        <div class="form-group">
                            <label for="username">Adresse email:</label>
                            <input type="email" id="username" class="form-control form-control-sm" name="_username" aria-describedby="emailHelp" value="{{ last_username }}" >
                        </div>

                        <div class="form-group">
                            <label for="password">Mot de passe:</label>
                            <input type="password" id="password" name="_password"  class="form-control form-control-sm"/>
                            <input type="hidden"  name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                            <input type="hidden" name="_target_path" value="{{ path('app_home') }}"/>
                            <button class="btn btn-outline-dark btn-lg px-5 mt-5" type="submit">Se connecter</button>
                             <input type="hidden" name="_target_path" value="/admin"/>
                        </div>
                    </form>
{% if error %}
        <div class="text-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}
                </div>
            </div>
        </div>
    </div>


</div> #}
{% endblock %}
